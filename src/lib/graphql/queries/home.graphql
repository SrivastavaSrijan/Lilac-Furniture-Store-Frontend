query HomePage($take: Int, $skip: Int) {
  banners {
    head
    href
    image {
      publicUrlTransformed
    }
    subtitle
    title
  }
  categories(take: $take, skip: $skip) {
    name
    description
    products {
      id
    }
  }
}