query HomePage($take: Int) {
  banners {
    head
    href
    image {
      publicUrlTransformed
    }
    subtitle
    title
  }
  categories(take: $take) {
    name 
    products {
      id
    }
  }
}